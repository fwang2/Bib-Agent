[project]
name = "bib-agent"
version = "0.1.0"
description = "Google Scholar driven BibTeX updater with strict manual/agent ownership boundaries."
requires-python = ">=3.11"

[tool.setuptools]
py-modules = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
